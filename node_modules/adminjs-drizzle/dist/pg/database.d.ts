import { BaseDatabase } from 'adminjs';
import { PgDatabase, PgQueryResultHKT, PgTableWithColumns, TableConfig } from 'drizzle-orm/pg-core';
import { Resource } from './resource.js';
export declare class Database extends BaseDatabase {
    private db;
    private schema;
    constructor(args: DatabaseConfig);
    resources(): Resource[];
    static isAdapterFor(args?: Partial<DatabaseConfig>): boolean;
}
interface DatabaseConfig {
    db: PgDatabase<PgQueryResultHKT>;
    schema: Record<string, PgTableWithColumns<TableConfig>>;
}
export {};
