import { BaseRecord, BaseResource, Filter } from 'adminjs';
import { PgDatabase, PgQueryResultHKT, PgTableWithColumns, TableConfig } from 'drizzle-orm/pg-core';
import { Property } from './property.js';
export declare class Resource extends BaseResource {
    private db;
    private table;
    private propertiesObject;
    private get idColumn();
    constructor(args: ResourceConfig);
    databaseName(): string;
    databaseType(): string;
    name(): string;
    id(): string;
    properties(): Property[];
    property(path: string): Property;
    build(params: Record<string, any>): BaseRecord;
    count(filter: Filter): Promise<number>;
    find(filter: Filter, params?: Record<string, any>): Promise<BaseRecord[]>;
    findOne(id: string | number): Promise<BaseRecord | null>;
    findMany(ids: Array<string | number>): Promise<BaseRecord[]>;
    create(params: Record<string, any>): Promise<Record<string, any>>;
    update(id: string | number, params?: Record<string, any>): Promise<Record<string, any>>;
    delete(id: string | number): Promise<void>;
    static isAdapterFor(args?: Partial<ResourceConfig>): boolean;
    protected prepareProperties(): Record<string, Property>;
    protected prepareParams(params: Record<string, any>): Record<string, any>;
    protected prepareResult(result: Record<string, any>): Record<string, any>;
}
interface ResourceConfig {
    table: PgTableWithColumns<TableConfig>;
    db: PgDatabase<PgQueryResultHKT>;
}
export {};
