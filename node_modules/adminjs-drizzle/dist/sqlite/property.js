import { BaseProperty } from 'adminjs';
import { SQLiteBigInt, SQLiteBlobBuffer, SQLiteBlobJson, SQLiteBoolean, SQLiteInteger, SQLiteNumeric, SQLiteNumericBigInt, SQLiteNumericNumber, SQLiteReal, SQLiteText, SQLiteTextJson, SQLiteTimestamp } from 'drizzle-orm/sqlite-core';
export class Property extends BaseProperty {
    column;
    columnPosition;
    _reference;
    constructor(path, column, columnPosition = 0, _reference) {
        super({ path });
        this.column = column;
        this.columnPosition = columnPosition;
        this._reference = _reference;
    }
    isEditable() {
        return !this.isId();
    }
    isId() {
        return !!this.column.primary;
    }
    isRequired() {
        return this.column.notNull;
    }
    isSortable() {
        return this.type() !== 'reference';
    }
    reference() {
        return this._reference ?? null;
    }
    availableValues() {
        return this.isEnum() ? this.column.enumValues : null;
    }
    position() {
        return this.columnPosition ?? 0;
    }
    isEnum() {
        const column = this.column;
        return column instanceof SQLiteText && column.enumValues?.length;
    }
    type() {
        if (this.reference()) {
            return 'reference';
        }
        const column = this.column;
        if (column instanceof SQLiteInteger) {
            return 'number';
        }
        if (column instanceof SQLiteReal
            || column instanceof SQLiteNumericNumber) {
            return 'float';
        }
        if (column instanceof SQLiteBigInt
            || column instanceof SQLiteBlobBuffer
            || column instanceof SQLiteBlobJson
            || column instanceof SQLiteNumeric
            || column instanceof SQLiteNumericBigInt
            || column instanceof SQLiteText
            || column instanceof SQLiteTextJson) {
            return 'string';
        }
        if (column instanceof SQLiteTextJson
            || column instanceof SQLiteBlobJson) {
            return 'textarea';
        }
        if (column instanceof SQLiteBoolean) {
            return 'boolean';
        }
        if (column instanceof SQLiteTimestamp) {
            return 'datetime';
        }
        console.warn(`Unhandled type: ${column.getSQLType()}`);
        return undefined;
    }
}
